<%= simple_form_for([@project, @project.goals.build]) do |f| %>

<% ref_id = goal_ref(@project.id) %>

<div class="form-inputs">
  <%= f.input :reference, input_html: {value: ref_id || 'AA'}  %>
</div>
  <div class="form-inputs">
    <%= f.input :problem %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
