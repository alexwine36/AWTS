<%= simple_form_for([@project, @project.goals.build]) do |f| %>


  <div class="form-inputs">
    <%= f.input :problem %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
