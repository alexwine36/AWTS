<div class="row">
  <%= simple_form_for(@page) do |f| %>
      <div class="form-inputs">
        <%= f.input :title %>
        <%= f.input :content, as: :text, input_html: { class: "materialize-textarea" }   %>
      </div>

      <div class="form-actions">
        <%= f.button :submit %>
      </div>
  <% end %>
</div>
